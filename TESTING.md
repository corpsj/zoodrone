# ZooDrone 테스트 가이드

이 문서는 ZooDrone 시스템의 주요 기능을 테스트하는 방법을 안내합니다.

## 사전 준비

```bash
# 1. 의존성 설치
pip install -r requirements.txt

# 2. 초기 설정 실행 (데이터베이스 + 테스트 데이터)
python setup.py

# 3. 애플리케이션 실행
python app.py
```

브라우저에서 http://localhost:5000 접속

## 1. 대시보드 테스트

### 확인 사항
- [ ] 전체 필지 수 표시
- [ ] 예정된 방제 계획 수 표시
- [ ] 완료된 작업 수 표시
- [ ] 최근 방제 계획 목록 표시
- [ ] 모든 링크가 정상 작동

### 테스트 방법
1. http://localhost:5000 접속
2. 대시보드에서 각 카드의 숫자가 정확한지 확인
3. "자세히 보기" 버튼 클릭하여 각 페이지로 이동 확인

## 2. 필지 관리 테스트

### 2.1 필지 목록 조회

#### 확인 사항
- [ ] 테스트 필지 3개가 목록에 표시됨
- [ ] 각 필지의 정보가 정확하게 표시됨 (이름, 소유주, 면적, 작물)
- [ ] 지도에 모든 필지가 표시됨

#### 테스트 방법
1. 상단 메뉴에서 "필지 관리" 클릭
2. 목록에서 필지 카드 확인
3. 지도에서 마커 확인

### 2.2 필지 검색

#### 확인 사항
- [ ] 필지명으로 검색 가능
- [ ] 소유주로 검색 가능
- [ ] 작물로 검색 가능
- [ ] 실시간으로 결과가 필터링됨

#### 테스트 방법
1. 검색창에 "테스트" 입력 → "테스트 필지 1" 표시 확인
2. 검색창에 "홍길동" 입력 → 홍길동 소유 필지만 표시 확인
3. 검색창에 "사과" 입력 → 사과 재배 필지만 표시 확인
4. 검색창 비우기 → 모든 필지 다시 표시 확인

### 2.3 필지 상세보기

#### 확인 사항
- [ ] 필지 카드 클릭 시 지도에 경계선 표시
- [ ] 해당 필지로 지도 줌인
- [ ] 경계선 색상 변경 (강조)

#### 테스트 방법
1. "테스트 필지 1" 카드 클릭
2. 지도에서 해당 필지의 경계선이 파란색으로 표시되는지 확인
3. 다른 필지 클릭 시 이전 강조 해제 확인

### 2.4 필지 등록

#### 확인 사항
- [ ] 모달 창이 정상 표시됨
- [ ] 모든 입력 필드가 작동함
- [ ] 필수 필드 검증이 작동함
- [ ] 경계선 그리기 기능 작동
- [ ] 저장 후 목록에 새 필지 추가됨

#### 테스트 방법
1. "필지 추가" 버튼 클릭
2. 모달 창에서 정보 입력:
   - 필지명: 새필지
   - 소유주: 테스터
   - 주소: 테스트 주소
   - 작물 종류: 포도
   - 면적: 7000
3. "경계선 그리기" 버튼 클릭
4. 지도에서 4개 이상의 점 클릭하여 다각형 생성
5. "그리기 완료" 버튼 클릭
6. "저장" 버튼 클릭
7. 목록에 새 필지가 추가되었는지 확인

### 2.5 필지 수정

#### 확인 사항
- [ ] 모달에 기존 정보가 채워짐
- [ ] 정보 수정 가능
- [ ] 저장 후 변경사항 반영됨

#### 테스트 방법
1. 필지 카드에서 "수정" 버튼 클릭
2. 필지명을 "수정된 필지명"으로 변경
3. "저장" 클릭
4. 목록에서 변경된 이름 확인

### 2.6 필지 삭제

#### 확인 사항
- [ ] 삭제 확인 메시지 표시
- [ ] 삭제 후 목록에서 제거됨
- [ ] 관련 방제 계획도 함께 삭제됨 (cascade)

#### 테스트 방법
1. 필지 카드에서 "삭제" 버튼 클릭
2. 확인 대화상자에서 "확인" 클릭
3. 목록에서 해당 필지가 사라졌는지 확인

## 3. 방제 계획 테스트

### 3.1 계획 목록 조회

#### 확인 사항
- [ ] 테스트 계획 6개가 목록에 표시됨
- [ ] 날짜순으로 정렬됨 (최신 순)
- [ ] 모든 정보가 정확하게 표시됨
- [ ] D-day가 정확하게 계산됨

#### 테스트 방법
1. 상단 메뉴에서 "방제 계획" 클릭
2. 목록에서 각 계획 확인
3. "오늘" 배지가 있는 계획 확인
4. "D-X" 배지가 있는 예정 계획 확인

### 3.2 계획 검색

#### 확인 사항
- [ ] 필지명으로 검색 가능
- [ ] 농약명으로 검색 가능
- [ ] 실시간 필터링

#### 테스트 방법
1. 검색창에 "다이센" 입력 → 다이센엠-45 계획만 표시
2. 검색창에 "테스트 필지" 입력 → 해당 필지의 계획만 표시
3. 검색창 비우기 → 모든 계획 표시

### 3.3 상태 필터

#### 확인 사항
- [ ] "전체" 버튼: 모든 계획 표시
- [ ] "예정" 버튼: 예정된 계획만 표시
- [ ] "완료" 버튼: 완료된 계획만 표시
- [ ] "취소" 버튼: 취소된 계획만 표시

#### 테스트 방법
1. "예정" 버튼 클릭 → 상태가 "예정"인 계획만 표시 확인
2. "완료" 버튼 클릭 → 상태가 "완료"인 계획만 표시 확인
3. "전체" 버튼 클릭 → 모든 계획 표시 확인

### 3.4 계획 등록

#### 확인 사항
- [ ] 모달 창이 정상 표시됨
- [ ] 필지 선택 드롭다운에 모든 필지 표시
- [ ] 필지 선택 시 필지 정보 표시
- [ ] 면적 기준 살포량 자동 계산
- [ ] 모든 입력 필드 작동
- [ ] 저장 후 목록에 추가됨

#### 테스트 방법
1. "계획 추가" 버튼 클릭
2. 필지 선택 → 자동으로 권장 살포량 계산 확인
3. 정보 입력:
   - 예정일: 내일 날짜
   - 농약명: 신규농약
   - 살포량: 자동 계산된 값 확인 또는 수정
   - 대상 병해충: 테스트병
   - 비고: 테스트 노트
4. "저장" 클릭
5. 목록에 새 계획 추가 확인
6. D-1 배지 표시 확인

### 3.5 계획 수정

#### 확인 사항
- [ ] 모달에 기존 정보 표시
- [ ] 수정 가능
- [ ] 저장 후 변경사항 반영

#### 테스트 방법
1. 계획의 "수정" 버튼 클릭
2. 농약명 변경
3. "저장" 클릭
4. 목록에서 변경 확인

### 3.6 상태 변경

#### 확인 사항
- [ ] "완료" 버튼으로 상태 변경 가능
- [ ] "취소" 버튼으로 상태 변경 가능
- [ ] 변경 후 즉시 목록 업데이트
- [ ] 상태 배지 색상 변경

#### 테스트 방법
1. 예정 상태 계획에서 "완료" 버튼 클릭
2. 상태가 "완료"로 변경되고 초록색 배지 표시 확인
3. 다른 계획에서 "취소" 버튼 클릭
4. 상태가 "취소"로 변경되고 빨간색 배지 표시 확인

### 3.7 계획 삭제

#### 확인 사항
- [ ] 삭제 확인 메시지 표시
- [ ] 삭제 후 목록에서 제거

#### 테스트 방법
1. 계획의 "삭제" 버튼 클릭
2. 확인 대화상자에서 "확인"
3. 목록에서 제거 확인

## 4. API 테스트 (선택사항)

### 4.1 필지 API

```bash
# 모든 필지 조회
curl http://localhost:5000/api/fields

# 특정 필지 조회
curl http://localhost:5000/api/fields/<field-id>

# 필지 생성
curl -X POST http://localhost:5000/api/fields \
  -H "Content-Type: application/json" \
  -d '{
    "name": "API 테스트 필지",
    "owner": "API 테스터",
    "address": "서울시",
    "crop_type": "딸기",
    "area": 3000,
    "center_lat": 37.5,
    "center_lng": 127.0,
    "boundary": [[37.49, 126.99], [37.51, 126.99], [37.51, 127.01], [37.49, 127.01]]
  }'
```

### 4.2 방제 계획 API

```bash
# 모든 계획 조회
curl http://localhost:5000/api/plans

# 예정된 계획만 조회
curl "http://localhost:5000/api/plans?status=scheduled"

# 특정 필지의 계획 조회
curl "http://localhost:5000/api/plans?field_id=<field-id>"

# 계획 생성
curl -X POST http://localhost:5000/api/plans \
  -H "Content-Type: application/json" \
  -d '{
    "field_id": "<field-id>",
    "scheduled_date": "2025-11-01",
    "pesticide_name": "API농약",
    "pesticide_amount": 20.0,
    "target_pest": "테스트병",
    "notes": "API 테스트"
  }'
```

## 5. 문제 해결

### 데이터베이스 리셋

```bash
# 데이터베이스 파일 삭제
rm data/zoodrone.db

# 다시 초기화
python setup.py
```

### 포트 충돌

app.py에서 포트 변경:
```python
app.run(host='0.0.0.0', port=5001, debug=True)
```

### 로그 확인

Flask 앱 실행 시 터미널에서 모든 요청과 오류 확인 가능

## 6. 테스트 체크리스트

### 필수 기능
- [ ] 대시보드 표시
- [ ] 필지 목록 조회
- [ ] 필지 등록
- [ ] 필지 수정
- [ ] 필지 삭제
- [ ] 필지 검색
- [ ] 지도에서 필지 표시
- [ ] 방제 계획 목록 조회
- [ ] 방제 계획 등록
- [ ] 방제 계획 수정
- [ ] 방제 계획 삭제
- [ ] 방제 계획 검색
- [ ] 상태 필터링
- [ ] 상태 변경
- [ ] 살포량 자동 계산
- [ ] D-day 계산

### 고급 기능
- [ ] 경계선 그리기
- [ ] 필지 강조 표시
- [ ] 토스트 알림
- [ ] 필드 정보 자동 표시
- [ ] 입력 검증

## 결과 보고

테스트 완료 후 발견된 버그나 개선사항을 이슈로 등록해주세요.

모든 기능이 정상 작동하면 Phase 3까지 완료된 것입니다! 🎉
